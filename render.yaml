services:
  - type: web
    name: pdf-vault
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app
    envVars:
      - key: PYTHON_VERSION
        value: "3.13"
